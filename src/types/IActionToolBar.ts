// Importações necessárias
import React from 'react';

export interface ActionToolbarProps {
  title: string;  // O texto à esquerda
  children?: React.ReactNode;  // Componentes filho, como botões ou links
}


